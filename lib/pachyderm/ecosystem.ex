defmodule Pachyderm.Ecosystem do
  @moduledoc """
  a biological community of interacting organisms and their physical environment.

  Have an Ecosystem.Supervisor started by Pachyderm application
  Then you can use {module, args} in application config same way as
  in local application
  The you can have one called default.

  NOTE: A start_link option will start a shard of the ecosystem.
  We might want a different way to reference the ecosystem as a whole

  Several other names were in the pipeline
  - world, space, web, mesh, workspace
  - population, cohort, hoard, mob, collective, gathering, gaggles
  - herd, pack, flock, swarm
  - scuttlebutt calls it a network
  """
end
